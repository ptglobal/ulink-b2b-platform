# Normalize line endings — repo stores LF; checkout adapts per-OS.
# Keeps Windows dev consistent with the Linux VPS / Vercel runtime.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
